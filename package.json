{
  "name": "pirate-claw",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "bin": {
    "pirate-claw": "./bin/pirate-claw"
  },
  "scripts": {
    "bootstrap:web": "bun install --cwd web --frozen-lockfile",
    "check:web": "bun run bootstrap:web && bun run --cwd web check",
    "ci": "bun run verify && bun run test && bun run test:web",
    "ci:quiet": "bun run ./scripts/ci-quiet.ts",
    "deliver": "bun run ./scripts/deliver.ts",
    "daemon:dev": "bun --watch ./src/cli.ts daemon",
    "format": "prettier --write . --ignore-path .prettierignore.root",
    "format:quiet": "bun run ./scripts/format-quiet.ts",
    "format:check": "prettier --check . --ignore-path .prettierignore.root",
    "format:check:web": "bun run bootstrap:web && bun run --cwd web format:check",
    "format:web": "bun run bootstrap:web && bun run --cwd web format",
    "format:web:quiet": "bun run ./scripts/format-web-quiet.ts",
    "hooks:install": "git config core.hooksPath .githooks",
    "lint": "bun --bun node_modules/eslint/bin/eslint.js .",
    "lint:web": "bun run bootstrap:web && bun run --cwd web lint",
    "run": "bun run ./src/cli.ts",
    "closeout-stack": "bun run ./scripts/closeout-stack.ts",
    "test": "TMPDIR=/tmp TMP=/tmp TEMP=/tmp bun test --timeout 20000",
    "test:coverage": "TMPDIR=/tmp TMP=/tmp TEMP=/tmp bun test --coverage --timeout 20000",
    "test:web": "bun install --cwd web --frozen-lockfile && bun run --cwd web test",
    "validate:mac:launchd": "bun run ./scripts/validate-mac-launchd.ts",
    "typecheck": "tsc --noEmit",
    "verify": "bun run format:check && bun run typecheck && bun run lint && bun run verify:web",
    "verify:quiet": "bun run ./scripts/verify-quiet.ts",
    "verify:web": "bun run bootstrap:web && bun run --cwd web format:check && bun run --cwd web lint && bun run --cwd web check"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "^1.3.13",
    "eslint": "^10.3.0",
    "globals": "^17.6.0",
    "prettier": "^3.8.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.59.2"
  },
  "dependencies": {
    "fast-xml-parser": "^5.7.3"
  }
}
